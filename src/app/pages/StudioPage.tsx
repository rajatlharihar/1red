import { StudioProcess } from '../components/StudioProcess';

/* /studio is one composition: the process as a print. The previous
   editorial page (hero, beliefs, team, principles, process rail, CTA)
   lived in components/Studio.tsx and is in git history if any of it is
   wanted back. */
export function StudioPage() {
  return <StudioProcess />;
}
